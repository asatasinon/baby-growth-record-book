{
  "name": "baby-growth-record-book",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "setup:backend": "cd backend && uv sync --extra dev",
    "setup:worker": "cd worker && uv sync --extra dev",
    "setup:python": "pnpm setup:backend && pnpm setup:worker",
    "db:create": "cd backend && uv run python scripts/db_admin.py create",
    "db:upgrade": "cd backend && uv run alembic -c alembic.ini upgrade head",
    "db:init": "cd backend && uv run python scripts/db_admin.py init && uv run alembic -c alembic.ini upgrade head",
    "db:seed": "cd backend && uv run python scripts/db_admin.py seed",
    "db:reset": "pnpm db:init && pnpm db:seed",
    "dev:miniapp": "pnpm --filter @baby-growth/miniapp dev:weapp",
    "dev:h5": "pnpm --filter @baby-growth/miniapp dev:h5",
    "dev:admin": "pnpm --filter @baby-growth/admin dev",
    "dev:backend": "cd backend && uv run python main.py",
    "dev:worker": "cd worker && uv run python -m app.main",
    "pm2:start:backend": "API_RELOAD=false pm2 start pnpm --name baby-backend -- run dev:backend",
    "pm2:start:h5": "pm2 start pnpm --name baby-h5 -- run dev:h5",
    "pm2:start:all": "pnpm pm2:start:backend && pnpm pm2:start:h5",
    "pm2:stop:backend": "pm2 stop baby-backend",
    "pm2:stop:h5": "pm2 stop baby-h5",
    "pm2:stop:all": "pnpm pm2:stop:backend && pnpm pm2:stop:h5",
    "pm2:logs:backend": "pm2 logs baby-backend",
    "pm2:logs:h5": "pm2 logs baby-h5",
    "pm2:list": "pm2 list",
    "build:h5": "pnpm --filter @baby-growth/miniapp build:h5",
    "build:weapp": "pnpm --filter @baby-growth/miniapp build:weapp",
    "build:admin": "pnpm --filter @baby-growth/admin build",
    "check:backend": "cd backend && uv run ruff check --fix app",
    "check:worker": "cd worker && uv run ruff check --fix app",
    "check": "pnpm check:backend && pnpm check:worker",
    "lint:backend": "cd backend && uv run ruff check app",
    "lint:worker": "cd worker && uv run ruff check app",
    "lint:frontend": "pnpm --filter @baby-growth/admin build && pnpm --filter @baby-growth/miniapp build:h5",
    "lint": "pnpm lint:backend && pnpm lint:worker && pnpm lint:frontend"
  },
  "devDependencies": {
    "pm2": "^6.0.14"
  }
}
