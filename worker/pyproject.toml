[project]
name = "baby-growth-worker"
version = "0.1.0"
description = "Background worker for baby growth platform"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "pydantic-settings>=2.6.0",
  "python-json-logger>=2.0.7"
]

[project.optional-dependencies]
dev = [
  "ruff>=0.6.8"
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]
